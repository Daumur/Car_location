@model IEnumerable<Car_Location.Models.Agency>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Localization)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Localization)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Agency }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Agency }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Agency })
        </td>
    </tr>
}

</table>
